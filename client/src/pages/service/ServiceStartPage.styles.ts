import styled from 'styled-components';

export const SubmitBtn = styled.button`
  font: inherit;
  border: 1px solid #0d2d31;
  font-weight: bold;
  padding: 0.75rem 3.5rem;
  cursor: pointer;
  font-size: 1.05rem;
  border-radius: 7px;
  justify-content: space-between;
  width: fit-content;
  margin: auto;
`;
